rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through Cloud Functions with admin SDK, which bypasses these rules.
    // Block all client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
